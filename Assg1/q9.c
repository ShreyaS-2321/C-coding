#include<stdio.h>

int main()
{
    float l,b;
    printf("Enter the value of lenght and breadth of the rectangle:");
    scanf("%f %f",&l,&b);
    printf("Area of rectangle is:%.2f",l*b);
    return 0;
}